<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wangzhen.dao.CheckItemDao">
    <insert id="add" parameterType="checkItem">
    insert into t_checkitem values (null ,#{code},#{name},#{sex},#{age},#{price},#{type},#{remark},#{attention})
</insert>
    <update id="edit" parameterType="checkItem">
        update  t_checkitem
        <set>
            <if test="code != null">
                code=#{code},
            </if>
            <if test="name != null">
                name=#{name},
            </if>
            <if test="sex != null">
                sex=#{sex},
            </if>
            <if test="age != null">
                age=#{age},
            </if>
            <if test="price != null">
                price=#{price},
            </if>
            <if test="type != null">
                type=#{type},
            </if>
            <if test="attention != null">
                attention=#{attention},
            </if>
            <if test="remark != null">
                remark=#{remark}
            </if>
        </set>
        where id=#{id}
    </update>
    <delete id="delect" parameterType="int">
        delete from t_checkitem  where id=#{id}
    </delete>

    <select id="selectByCondition" parameterType="string" resultType="checkItem">
        select * from t_checkitem
        <if test="value != null and value.length >0">
            where code=#{value} or name=#{value}
        </if>
    </select>
    <select id="findCountByCheckItemId" parameterType="int" resultType="long">
        select count(*) from t_checkgroup_checkitem where  checkitem_id=#{id}
    </select>
    <select id="findById" resultType="checkItem" parameterType="int">
        select * from t_checkitem where id=#{id}
    </select>
    <select id="findAll" resultType="checkItem">
        select  * from  t_checkitem
    </select>

    <select id="findByIdItemById" parameterType="int" resultType="checkItem">
        SELECT * FROM t_checkitem WHERE id IN (SELECT checkitem_id FROM  t_checkgroup_checkitem WHERE checkgroup_id =#{id})
    </select>
</mapper>